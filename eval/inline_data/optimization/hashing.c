/**
 * Generated by LLM (DeepSeek R1) with the SpecFS framework.
 * 
 * The concrete prompt for this file and hashing.h is:

[SPECIFICATION]
(* Coq formalization of the multiplicative hash function *)
Definition seed : nat := 131. (* Multiplicative seed *)
Definition mask : nat := 0x1ff. (* 9-bit mask *)

Fixpoint hash_spec (name : list char) : nat :=
match name with
| [] => 0
| c :: rest => (hash_spec rest * seed + (nat_of_ascii c)) mod (mask + 1)
end.

(* Postcondition: Output is within [0, 511] *)
Lemma hash_range : forall (s : list char), hash_spec s <= mask.
Proof. (* ... *) Admitted.

[CONTEXT]
Implement a deterministic hash function for a file system directory. The function maps filenames (C strings) to 9-bit integers (0–511) for use in a hash table. Must be lightweight and thread-safe.

[Rely]
- **Input**: `name` is a valid null-terminated C string.
- **Operations**: Only pointer arithmetic, bitwise masking, and standard C integer operations.
- **No external libraries**: Self-contained implementation.

[Guarantee]
1. **Output range**: `hash_func(name) ∈ [0, 511]`.
2. **Determinism**: Same input → same output.
3. **No side effects**: Does not modify `name`.
4. **Exposed API**:
```c
unsigned int hash_func(char* name); // Defined in hashing.h
```

[PROMPT]
Generate a **complete C implementation** of `hash_func` that:
1. Uses the multiplicative hash algorithm: `hash = hash * 131 + current_char`.
2. Applies a final mask `0x1ff` to constrain output to 9 bits.
3. Processes the entire string until `\0`.

Provide both `hashing.h` (header) and `hashing.c` (source) as separate code blocks. Ensure:
- No memory leaks or external dependencies.
- Strict adherence to the function signature.
- Portable C99-compliant code.

 * 
 */

#include "hashing.h"

unsigned int hash_func(char* name) {
    unsigned int hash = 0;
    while (*name != '\0') {
        hash = hash * 131 + *name;
        name++;
    }
    return hash & 0x1ff;
}